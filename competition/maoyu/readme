三个源代码的执行顺序是
1.预处理.cpp
2.初步生成数据.cpp
3.对数据进行最后调整.cpp

我本人使用的是Visual Studio 2017编写的，但实际上使用其他软件编译应该也没有什么大问题。
要是用Visual Studio 2017的话注意把SDL检查关掉不然会报错。

首先将提供的数据中的11.csv文件放入 预处理 程序工程目录下或编译生成的可执行文件目录下编译运行，得到11.out.csv
然后将 11.out.csv 放入 初步生成数据 程序工程目录下或编译生成的可执行文件目录下编译运行，得到12.out.csv
最后将 12.out.csv 放入 对数据进行最后调整 程序工程目录下或编译生成的可执行文件目录下编译运行，得到12.final.csv

注意，由于未知的原因（我也不知道为什么），程序在退出的时候会因为访问权限报错（可执行文件的话会停止运行），直接无视停止运行即可，不影响生成数据。

由于比赛时的失误，我提交的最高分数据实际上等同于 对数据进行最后调整.cpp 文件中第42，62行的24552改成23760得到的，如果当时就写成24552应该得到的分数更高（第一不敢保证，再前进个一两名是妥妥的）。

当然因为我比较懒实际上我提交上去的文件不是通过 对数据进行最后调整.cpp 生成的，这是事后为了方便交源代码写的。我是通过 12.final(excel实现).xlsx 中的方法来处理 12.out.csv 的数据，通过调整D1的值使J2的值逼近J3，再用E列中的数据替换掉C列中的数据，去除多余数据，得到的就是我提交的最高分数据。
但是这个调整模板是从11月的改过来的当时I2的分母忘改了所以误差就大了些⋯⋯改成24552效果该更好的⋯⋯

希望能看看正确生成的12.final.csv能获得的分数有多少⋯⋯

所有中间过程的文件都放在这个文件夹里了，可执行文件也放进去了，代码要是因为奇怪的原因运行不了可以直接使用这个⋯⋯
